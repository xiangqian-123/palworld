# Palworld 站点部署说明

> 2026-08-23 更新：已从零搭建 **Next.js 项目骨架**，第 5 步「部署 MDX」已完成（18 页写入 content/guides）。
> 现在按课程模块 06 的关卡 5「部署上线」继续：接 GA → 推 GitHub → Vercel 部署 → GSC → 绑域名。

## 文件结构

```
Palworld/                        # ★ 网站项目根目录
├── app/                         # Next.js App Router
│   ├── layout.tsx               # 根布局（已注入 GA4 脚本）
│   ├── globals.css              # 全局样式 + cyan 主题变量
│   ├── page.tsx                 # 根页 → 重定向 /zh-CN
│   ├── sitemap.ts / robots.ts   # SEO（动态读 siteUrl，无残留）
│   └── [locale]/
│       ├── layout.tsx           # 导航 + 页脚
│       ├── page.tsx             # 首页（Hero/卡片/介绍/CTA）
│       ├── guide/[slug]/page.tsx  # 攻略页（MDX 渲染）
│       ├── privacy/page.tsx     # 隐私政策
│       └── terms/page.tsx       # 服务条款
├── components/                  # Nav / Footer / GoogleAnalytics
├── lib/                         # locales / posts / i18n / site
├── i18n/messages/               # zh-CN / en / ja / ru / de
├── scripts/generate-site.cjs    # 部署脚本
├── site.config.ts               # 站点配置中心（gaId 读环境变量）
├── content/guides/              # 已生成（zh-CN + zh-TW）
├── input/zh-CN/                 # 18 个 MDX 源文件
├── public/images/guides/        # hero 配图（待放图）
└── .env.local.example           # GA 环境变量示例
```

## 第 5 步（已完成）✅

```bash
node scripts/generate-site.cjs input/zh-CN zh-CN
# → 18/18 写入 content/guides/zh-CN + 18 个繁体版 zh-TW
```

---

## 关卡 5：部署上线操作清单（按课程模块 06）

### 前置：把代码推到 GitHub（关卡 4 收尾）

1. 去 github.com → New repository，起个名（如 `palworld-wiki`），**不要**勾选生成 README，设为 private。
2. 本地推送（可让 AI 帮忙，或自己跑）：
```bash
cd Palworld
git init
git add .
git commit -m "init palworld wiki"
git branch -M main
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

### 步骤 1：拿 GA 衡量 ID

1. 用 Gmail 登录 https://analytics.google.com/analytics/web/
2. 新建「账号」→ 建「媒体资源」→ 数据流选 **Web**，填网站地址
3. 拿到一串 `G-` 开头的衡量 ID（这就是 `NEXT_PUBLIC_GA_ID` 的值）

### 步骤 2：填 GA 环境变量

- **本地验证**：复制 `.env.local.example` 为 `.env.local`，填 `NEXT_PUBLIC_GA_ID=G-你的ID`
- **Vercel 上线**：部署时在 Vercel 项目 → Settings → Environment Variables 里加同一个键值（`.env.local` 已被 .gitignore 排除，不会进仓库）

> 代码已改好：`site.config.ts` 的 `gaId` 读环境变量，`components/GoogleAnalytics.tsx` 负责注入 GA4 脚本，`app/layout.tsx` 已接入。GA ID 为空时自动跳过、不影响本地运行。

### 步骤 3：改 siteUrl（sitemap/robots 用的正式网址）

`site.config.ts` 里 `siteUrl` 当前是占位 `https://palworld-wiki.vercel.app`。上线时改成你的真实地址（Vercel 默认域名 `<项目名>.vercel.app` 或自己买的域名）。

> ⚠️ 课程防坑点：复用模板上线前，务必确认 sitemap/robots 里没有旧站 URL 残留。本项目 sitemap.ts/robots.ts 都是动态读 `siteUrl`，改这一处即全局生效，无残留。

### 步骤 4：Vercel 部署

1. 去 vercel.com 注册（可用 GitHub 登录），新建 Team → Add New → Project
2. Import 你刚推的 GitHub 仓库
3. 框架会自动识别为 Next.js，不用改
4. 在 Environment Variables 里填 `NEXT_PUBLIC_GA_ID`
5. 点 Deploy，等几分钟 → 得到 `<项目名>.vercel.app` 线上地址

### 步骤 5：接 GSC（Google Search Console）

1. 登录 https://search.google.com/search-console/about
2. 添加资源，输入你的域名（先用 Vercel 默认域名练手即可）
3. 验证 → 提交 `sitemap.xml` 索引

### 步骤 6：绑自定义域名（可选，纯新手可跳过）

1. 在 Vercel → Settings → Domains → Add Domain，输入你买的域名
2. 拿到 Vercel 给的 A 记录和 CNAME 记录
3. 到 Cloudflare → DNS → Add Records，分别添加 A（Name=@）和 CNAME（Name=www）

### 步骤 7：上线后 SEO 自查（三项）

- **canonical**：页面 <head> 里有正确的 canonical 标签
- **301 重定向**：www 和非 www 统一到一个
- **HTTPS**：地址栏有锁图标

---

## 待办清单

- [ ] 创建 GitHub 仓库并推送代码
- [ ] 拿 GA 衡量 ID（G- 开头），填环境变量
- [ ] site.config.ts 的 siteUrl 改成真实域名
- [ ] Vercel 部署上线
- [ ] GSC 验证 + 提交 sitemap
- [ ] 准备 18 张 hero 配图（public/images/guides/）
- [ ] （可选）绑自定义域名 + Cloudflare DNS
